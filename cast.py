def _init(self):
    self._actors={}

def add_add_actor(self,group,actor):
    if not group in self._actors.Keys():
        self_actors[group]=[]
        if not actor in self._actors[group]:
            self._actors[group].append(actor)



